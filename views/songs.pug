extends layout

block content
  h1= title
  p Welcome to #{title}
  ul
    each val in songs
      li 
        h4= val.title
        p tuning: #{val.tuning}  
        p rating: #{val.rating}  
        a(href=val.link) Link to chords